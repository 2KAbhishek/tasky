import './Header.css';
import Button from '../Button/Button';

const Header = () => {
  return (
    <header className="Header">
      <h1>Tasky</h1>
      <Button/>
    </header>
  );
};

export default Header;
